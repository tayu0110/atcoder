// TODO
// use proconio::*;
// use segtree::SegmentTree;
// use std::cmp::max;

fn main() {
    // input! {n: usize, q: usize}

    // let mut st = SegmentTree::new(n, 0usize, |l, r| max(*l, *r));
    // for _ in 0..q {
    //     input! {ty: usize}

    //     if ty == 1 {
    //         input! {pos: usize, x: usize}
    //         st.set(pos - 1, x);
    //     } else {
    //         input! {l: usize, r: usize}
    //         println!("{}", st.foldl(l - 1, r - 1))
    //     }
    // }
}
