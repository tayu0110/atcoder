// TODO
// use proconio::*;
// use segtree::SegmentTree;
// use static_modint::{Mod1000000007, StaticModint};

// type Modint = StaticModint<Mod1000000007>;

fn main() {
    // input! {n: usize, k: usize}

    // let mut ft0 = SegmentTree::new(n + 1, Modint::zero(), |&l, &r| l + r);
    // let mut ft1 = SegmentTree::new(n + 1, Modint::zero(), |&l, &r| l + r);
    // ft0.set(0, Modint::one());

    // for i in 1..=n {
    //     let (s, t) = (ft0.foldl(i.saturating_sub(k - 1), i), ft1.foldl(0, i));
    //     ft0.set(i, t);
    //     ft1.set(i, s);
    // }

    // println!("{}", ft1.foldl(n, n + 1))
}
