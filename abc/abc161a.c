#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdbool.h>

typedef long long ll;
typedef long double ld;

int main(int argc, char **argv) {
  int x, y, z;
  scanf("%d%d%d", &x, &y, &z);
  printf("%d %d %d\n", z, x, y);
  return 0;
}