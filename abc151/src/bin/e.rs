// TODO
// use proconio::*;
// use static_modint::{combination, Mod1000000007, StaticModint};

// type Modint = StaticModint<Mod1000000007>;

fn main() {
    // input! {n: usize, k: usize, mut a: [i64; n]}
    // a.sort();

    // let com = combination(n as u32 + 10);
    // let mut res = Modint::zero();
    // for i in 0..n {
    //     let now = Modint::from(a[i]);
    //     if i >= k - 1 {
    //         res += com(i, k - 1) * now;
    //     }
    //     if n - i >= k {
    //         res -= com(n - i - 1, k - 1) * now;
    //     }
    // }

    // println!("{}", res)
}
