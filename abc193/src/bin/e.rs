#![allow(unused_imports)]
use proconio::{
    input,
    marker::{Bytes, Chars},
    *,
};

fn main() {
    input! {t: usize}

    for _ in 0..t {
        input! {_x: usize, _y: usize, _p: usize, _q: usize}
    }
}
