#![allow(unused_imports)]
use proconio::{
    input,
    marker::{Bytes, Chars},
    *,
};

fn main() {
    input! {t: usize}

    for _ in 0..t {
        input! {x: usize, y: usize, p: usize, q: usize}
    }
}
